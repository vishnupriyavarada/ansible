- name : print variables from prompt
  hosts : local
  connection : local
  vars : # variables from prompt
    - name : COURSE
      prompt : what's the course name ?
      private : false
    - name : DURATION
      prompt : what's the duration ?
      private : false
    - name : TRAINER
      prompt : what's the trainer name ?
      private : false

  tasks :
    - name : print course information through prompt
      msg :  "Course is {{COURSE}}. Duration is {{DURATION}}. Trainer is {{ TRAINER}}"