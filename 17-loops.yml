- name : loops
  hosts : local
  connection : local
  tasks :
    - name : Print names
      ansible.builtin.debug:
        msg : "Hello {{ item }}"
      loop :
      - Krishna
      - Vishnu
      - Veena